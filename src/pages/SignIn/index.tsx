import { StatusBar, Text, View } from "react-native";

export default function SignIn() {
  return (
    <View style={{ paddingTop: StatusBar.currentHeight }}>
      <Text>Página SignIn</Text>
    </View>
  );
}
